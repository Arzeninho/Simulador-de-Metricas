# Configuración del servidor
PORT=4000

# Base de datos PostgreSQL (Render)
# Usa la URL completa que te da Render en "Connection Details"
# Formato: postgres://usuario:password@hostname:5432/nombre_base
DATABASE_URL=postgres://your-user:your-password@your-host.renderinternal:5432/your-database

# Secret para JWT
JWT_SECRET=your-secret-key-here
